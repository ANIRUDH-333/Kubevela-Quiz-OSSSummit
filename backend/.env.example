# Quiz Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Domain Configuration for Production Deployments
# Frontend URL - Set this to control which domain the OAuth callbacks redirect to
# Leave empty to auto-detect from request or use default logic
# For kubevela.guidewire.co.in deployment, set: FRONTEND_URL=https://kubevela.guidewire.co.in
# For Vercel deployment, set: FRONTEND_URL=https://kubevela-quiz-oss-summit.vercel.app
FRONTEND_URL=

# API Base URL - Set this if your API is hosted at a specific domain
# Leave empty to auto-detect or use default logic  
# For kubevela.guidewire.co.in deployment, set: API_BASE_URL=https://kubevela.guidewire.co.in/api
# For Vercel deployment, set: API_BASE_URL=https://kubevela-quiz-oss-summit.vercel.app/api
API_BASE_URL=

# Session Secret (change in production)
SESSION_SECRET=your-secret-key-change-in-production

# Google OAuth Configuration
# Create credentials at: https://console.developers.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth Configuration  
# Create OAuth app at: https://github.com/settings/applications/new
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Google Sheets Configuration
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SHEETS_RANGE=Sheet1!A:G
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# Instructions:
# 1. Replace 'your_spreadsheet_id_here' with your actual Google Sheets ID
# 2. Set up a Google Service Account and download the JSON key file
# 3. Update the path to your service account key file
# 4. Make sure your Google Sheet has the following columns:
#    A: Question
#    B: Option 1
#    C: Option 2
#    D: Option 3
#    E: Option 4
#    F: Correct Answer (0-3, where 0 is Option 1)
#    G: Weightage (numeric value)
